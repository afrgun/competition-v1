# API Configuration
NEXT_PUBLIC_API_BASE_URL=https://your-api-url.com

# Environment
NODE_ENV=development

# Mock Authentication - Skip real API calls for login (for UI slicing)
NEXT_PUBLIC_USE_MOCK_AUTH=true

# Developer Mode - Set user role manually for UI slicing (remove when backend API ready)
NEXT_PUBLIC_DEV_MODE=true
NEXT_PUBLIC_DEV_ROLE=employee
# Options: employee | admin
